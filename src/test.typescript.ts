function getConf() {
  return {
    name: "my conf",
    x: 123,
    ready: true
  };
}

const conf = getConf();
// console.info(conf.nume);

//conf.ready = "not ready";

function a(b) {
  return b * 2;
}
